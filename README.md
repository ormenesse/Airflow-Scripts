# Instructions

This will create a ariflow cluster inside a VM.

* Create a folder `/opt/airflow`.
* Create subfolders:
  - config
  - dags
  - dbs
  - logs
  - plugins

Run command:
`docker-compose up -d`
